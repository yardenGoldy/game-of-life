export const serverAPIPort = 3500;
export const host = 'http://localhost'
export const TicketAPIDomain = 'game';
export const TicketAPIRootPath = `${host}:${serverAPIPort}/${TicketAPIDomain}/`
export const staticsPort = 3000;
export const staticsUrl = `${host}:${staticsPort}/`;
